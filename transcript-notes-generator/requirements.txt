streamlit>=1.32.0
faster-whisper>=1.0.0
numpy>=1.26.0
scipy>=1.13.0
httpx>=0.27.0
soundcard>=0.4.4
soundfile>=0.12.1
pyaudiowpatch>=0.2.12

# ── Prerequisites (not pip-installable) ──────────────────────────────────────
# 1. GitHub CLI installed and authenticated:
#      gh auth login
#    OR set GITHUB_TOKEN environment variable.
#
# 2. faster-whisper downloads ~145 MB on first use (base model).
#    Models are cached in ~/.cache/huggingface/hub/
#    No API key required — runs fully locally.
#
# 3. st.audio_input requires Streamlit >= 1.32.0
#
# 4. soundcard uses WASAPI loopback on Windows for system audio capture.
#    No extra drivers needed — works with Intel/Realtek/Conexant sound drivers.
